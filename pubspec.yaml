name: budgettrip_ai
description: Met U - 예산 기반 여행 추천 앱 (Flutter)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  dio: ^5.4.0
  file_picker: ^8.1.7
  url_launcher: ^6.3.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  flutter_web_auth_2: ^5.0.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
